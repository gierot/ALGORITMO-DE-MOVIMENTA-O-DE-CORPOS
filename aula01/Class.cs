using System;

namespace aula01
{
    using System;
    using System.IO;

    namespace ManipulacaoArquivo
    {
        class Program
        {
            /*static void Main(string[] args)
            {
                if(File.Exists("teste.txt"))
                {
                     string conteudo = File.ReadAllText("teste.txt");
                     Console.WriteLine("Conteudo do arquivo:");
                     Console.WriteLine(conteudo);
                 }
                 string newContent = null;
                 while(string.IsNullOrWhiteSpace(newContent) || newContent.ToLower().Replace(" ", "") != "sair")
                 {
                     Console.WriteLine("Favor entrar com novo conteúdo para o arquivo:");
                     newContent = Console.ReadLine();
                     if(newContent.ToLower() != "sair")
                     {
                         File.AppendAllText("teste.txt", newContent+'\n');
                     }
                 }
                File.WriteAllText("teste.txt", newContent);*/
            }
        }
    }

